import { FastifyInstance } from 'fastify';

export default (app: FastifyInstance) => {
  app.get('/waitlist', (req, res) => {
    res.redirect('https://zjyparvu55g.typeform.com/to/E9YoNwYz');
  });
};
