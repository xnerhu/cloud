datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
  output   = "gen"
}

model Release {
  id   Int    @id @default(autoincrement())
  name String
  aha  String
}
