{
  "name": "wexond-cloud",
  "private": true,
  "scripts": {
    "dev": "lerna run --stream --parallel dev",
    "analyze": "lerna run --stream --parallel analyze",
    "start": "lerna run --stream --parallel start",
    "build": "lerna run --stream --parallel build",
    "gen": "lerna run --stream --parallel gen",
    "link": "npx lerna link --local"
  },
  "devDependencies": {
    "@types/node": "^14.14.10",
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "@typescript-eslint/eslint-plugin": "^4.21.0",
    "@typescript-eslint/parser": "^4.21.0",
    "concurrently": "^5.3.0",
    "cross-env": "^7.0.3",
    "eslint-config-prettier": "^7.0.0",
    "eslint-plugin-prettier": "^3.2.0",
    "eslint-plugin-react": "^7.23.1",
    "eslint-plugin-react-hooks": "^4.2.0",
    "lerna": "^3.22.1",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2",
    "ts-loader": "^8.0.11",
    "typescript": "^4.1.2"
  },
  "dependencies": {},
  "workspaces": [
    "packages/*"
  ]
}
